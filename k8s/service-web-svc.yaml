# Service skeleton for web-svc
apiVersion: v1
kind: Service
metadata:
  name: web-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      # nodePort: 30080 # optional; omit to auto-assign

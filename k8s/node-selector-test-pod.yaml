apiVersion: v1
kind: Pod
metadata:
  name: node-selector-test
  namespace: default
  labels:
    app: node-selector-test
spec:
  nodeSelector:
    pool: default
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80
